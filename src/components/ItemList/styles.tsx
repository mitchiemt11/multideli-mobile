import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  itemImage: {
    width: 60,
    height: 60,
    borderRadius: 5,
  },
  container: {
    flexDirection: 'row',
    margin: 20
  },
  itemContainer: {
    marginRight: 10,
  }
});

export default styles;